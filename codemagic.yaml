workflows:
  default:
    name: Default Workflow
    environment:
      flutter: stable
      java: 17
    cache:
      cache_paths:
        - $HOME/.pub-cache
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --debug
    artifacts:
      - build/**/outputs/flutter-apk/*.apk